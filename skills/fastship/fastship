#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PROJECT_ROOT="$(cd "$SCRIPT_DIR/../.." && pwd)"
export FASTSHIP_REPO_ROOT="${FASTSHIP_REPO_ROOT:-$PROJECT_ROOT}"
exec python3 "$SCRIPT_DIR/fastship_orchestrator.py" "$@"
